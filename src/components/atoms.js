import { atom } from "recoil";

export const refsidebar = atom({
  key: "refsidebar",
  default: null,
});

export const ison = atom({
  key: "ison",
  default: false,
});

export const userdata = atom({
  key: "userdata",
  default: null,
});

